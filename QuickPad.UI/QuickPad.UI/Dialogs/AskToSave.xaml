<ContentDialog
    x:Class="QuickPad.UI.Dialogs.AskToSave"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuickPad.UI.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Quick Pad"
    PrimaryButtonText="Save"
    SecondaryButtonText="Don't Save"
    CloseButtonText="Cancel"
    PrimaryButtonCommand="{StaticResource QuickPadCommands.SaveCommand}"
    PrimaryButtonCommandParameter="{Binding ViewModel}"    
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    Background="{x:Bind VisualThemeSelector.CurrentItem.BackgroundAcrylicBrush, Mode=OneWay}">

    <Grid>
        <TextBlock Text="Would you like to save your changes?"/>
    </Grid>
</ContentDialog>
